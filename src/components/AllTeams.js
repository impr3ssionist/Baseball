import React from "react";
import { BrowserRouter, Route } from "react-router-dom";

//first get a list of all teams to render to the page

const AllTeams = (props) => {
  return (
    <div id="teams-view">
      <h3> All teams view </h3>
    </div>
  );
};

export default AllTeams;
